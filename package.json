{
  "name": "kakeibo-st",
  "private": true,
  "version": "0.0.0",
  "description": "Asset management for Flask templates (Tabulator)",
  "scripts": {
    "postinstall": "node scripts/copy_tabulator.js",
    "assets": "npm install"
  },
  "dependencies": {
    "tabulator-tables": "5.6.2"
  }
}

